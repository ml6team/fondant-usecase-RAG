name: Text cleaning component
description: Clean text passages
image: text-cleaning-component:latest

consumes:
  text:
    fields:
      data:
        type: string

produces:
  text:
    fields:
      data:
        type: string
